import { defineConfig } from 'astro/config';

// https://astro.build/config

export default defineConfig({
    site: 'https://MayssaEssid.github.io',
    outDir: '../dist'  // Chemin de sortie explicite
  })